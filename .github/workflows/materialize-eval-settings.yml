name: Materialize evaluation-only Benchmark settings
on:
  push:
    branches: ['feature/benchmark-eval-settings-2026-09-20']
    paths:
      - 'scripts/build/benchmark_settings.py'
      - 'tests/test_benchmark_settings.py'
      - 'site/js/features/research/benchmark-settings.js'
permissions:
  contents: write
concurrency:
  group: eval-settings-materialize
  cancel-in-progress: false
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Rebuild generated Setting data and validate offline
        run: |
          set -eu
          python scripts/build/build_catalog.py
          python scripts/validate/validate_all.py
      - name: Remove one-shot audit/materialization files
        run: |
          rm -f .github/scripts/audit_eval_settings.py
          rm -f .github/workflows/audit-eval-settings.yml
          rm -f .github/workflows/materialize-eval-settings.yml
      - name: Commit deterministic generated outputs
        env:
          BRANCH: ${{ github.ref_name }}
          EXPECTED: ${{ github.sha }}
        run: |
          set -eu
          actual=$(git ls-remote origin "refs/heads/$BRANCH" | cut -f1)
          test "$actual" = "$EXPECTED" || exit 1
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Materialize evaluation-only Benchmark Settings'
          git push origin "HEAD:refs/heads/$BRANCH"
